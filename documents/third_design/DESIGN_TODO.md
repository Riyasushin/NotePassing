# NotePassing V3 Design TODO

> 只保留当前仍需决策或推进的事项。  
> 已经收口的结论不再展开写成长文。

## 已收口

- block 接口统一为 `/api/v1/block`
- 消息补漏统一为 `/api/v1/messages/sync` + `after`
- `third_design/` 作为当前默认文档源
- 设置页支持 tag 编辑并随资料同步上报
- Nearby 卡片显示 tag，不再显示 profile
- Nearby / Friends 支持点击头像查看资料详情弹窗
- tag 输入格式统一为 `#标签 #标签`，设置页自动补 `#`，并支持后台相同 tag 震动提醒
- Nearby 新增 tag match 悬浮名片队列：左滑忽略当前，右滑进入聊天但不出队，右上角可关闭全部；同一设备本次启动只记录一次

## 建议执行顺序


已完成队列
1. 先冻结匿名显示规则 ok
2. 再补加好友闭环 ok
2.5. 头像功能 ok
3. 然后上点击头像进入 Profile tag详情 页面 ok

5. 增加tag，并且优化显示在卡片上 ok
6. 相似tag震动提醒 ok
6. 改为显示蓝牙强度 ok

7. 后台优化，持续扫描+广播
4. 最后做头像展示与 UI 统一：去除测试功能、增加ui辨识度差异

7. codex网站demo实时显示蓝牙拓扑网络架构


后台优化 ok
附近聊天卡片、好友卡片风格化1 ok

- 待办队列：
app图标设计（l）
后台网站优化（l）
加入好友重逢消息
加入tagmatch特效
附近聊天卡片、好友卡片风格化2
好友组件、block功能前端优化
设置页逻辑优化
导航页逻辑优化

加入蓝牙网络显示功能？


- 详细prompt队列：

同时在每次启动软件进入附近页且有新的tag match的时候，会有tag match悬浮名片页（卡片和点击头像的显示相同，除了增加“左滑红色插，右滑有聊天气泡”的提示表示），右滑和他进入聊天界面，退出聊天界面之后悬浮名片页依然存在，左滑动忽略下一个，右上角×关闭所有悬浮名片页。对方2h重新进入附近且再次 tag match后不记录 ok

如果你愿意，我下一步可以把这张悬浮名片再做成更像卡组的动画切换，而不是现在的单卡滑动切页。（暂时不用，节约时间）

附近聊天、好友卡片风格化：离开后为深灰色，聊天即将失效为浅灰色，附近聊天为浅灰色背景+绿色边框 ok
朋友的聊天朋友为白色，同时在附近且为朋友的用户触发boost为白色+金色边框 ok
共同tag match的附近陌生用户会有浅灰色+左边半蓝右边半绿边框，tag match的离开后的陌生用户会有深灰色+蓝色边框，聊天即将失效为浅灰色+蓝色边框 ok



修改蓝牙强度为：远、中、近、很近ok
如果对方超出距离，把蓝牙强度改成”已超出距离“ok
增加附近和好友页的蓝色、绿色、金色提示条的厚度ok


卡片第二次设计：
卡片有三种背景颜色：很深的黑色，淡灰色，白色
卡片有三种边框：绿色、蓝色、金色（均比较厚）
附近页显示逻辑：很深的黑色（超距聊天已经失效），灰色（超距聊天没有失效），白色（距离内）；绿色（距离内）、蓝色（tag）match、金色（好友）。逻辑，绿色和蓝色若同时满足，则两个边框平行放置（两倍厚度），金色出现则覆盖所有其他颜色；附近的好友不用星星，用金色圆角矩形里面白色“好友”两个字代替
好友页的设置：距离内则为白色背景+金色加上金色圆角矩形里面白色”在附近“；好友在距离外则为白色背景。


app背景风格：
附近页为灰色
好友页为白色
字体做到标题栏字体和背景重复了，不用做自适应，统一风格


我希望你尝试优化这个app的功能，使得app在后台甚至在手机息屏的时候仍然能进行扫描和广播，可以通过通知栏实现，显示”雷达持续运行中“，tag匹配消息展现为”你通过#xxx有n条新的匹配“，好友重逢消息展示为你和xxx重逢了”。

去除软件附近页和好友页的测试端口


- debug队列
目前tag match的人无法加好友，按加好友，变成“发送中”，快速闪为“加好友” ok
目前头像上传之后，显示上传中，url更新但是双方都没有显示头像
目前统一所有不同系统下的字体颜色：



**推荐**

## Later：UI 统一

- 统一颜色、圆角、间距、字体层级
- 抽 Avatar / NearbyCard / FriendCard / ProfileHeader 公共组件
- 最后再做动画和空状态
