---
description: 开发工作流程
alwaysApply: true
---

# 开发工作流

## 协作模式

- AI 在 Cursor 中写代码
- 用户在 Android Studio 编译运行 App，反馈日志/截图/体验
- AI 根据反馈修复，循环迭代

## 双环境操作

| 操作 | Android App (`gxs/develop`) | Backend (`ljx/dev`) |
|------|------|------|
| 编辑 | Cursor 直接改 `android-app/` | Cursor 切分支改 `backend/` |
| 运行 | 用户在 Android Studio Rebuild & Run | 用户在 Linux 端 `git pull && 重启 uvicorn` |
| 调试 | Logcat + App 内 Debug 面板 | 服务器终端日志 |

- 改完 backend 代码后，提醒用户去 Linux 服务器拉取部署
- 改完 android 代码后，提醒用户在 Android Studio 重新编译
- 本机**没有 Python 环境**，不要尝试运行 backend

## 代码修改原则

- 改动前先读文件，理解上下文
- 尽量编辑已有文件，不随意新建
- 不删除用户已有代码，除非明确要求
- 修改 backend 时**不要改动日志生成部分**

## ⚠️ API 契约变更流程

- 写网络层代码时，必须打开契约文档核对字段和路径
- **绝不自行修改 API 路径、字段名、消息类型**
- 若发现契约有问题，立即高亮告知用户，用户确认后才能继续
