---
description: 开发工作流程
alwaysApply: true
---

# 开发工作流

## 协作模式

- AI 负责写代码，用户在 Android Studio 上编译运行
- 用户反馈：报错日志、运行截图、体验问题
- AI 根据反馈修复，循环迭代

## 开发节奏

1. 按 `documents/android_dev_plan.md` 中的 Phase 顺序推进
2. 每个 Phase 完成后告知用户验证点，等待用户反馈
3. 收到"通过"后再进入下一个 Phase
4. 遇到编译错误优先修复，不跳过

## 代码修改原则

- 改动前先读文件，理解上下文
- 尽量编辑已有文件，不随意新建
- 新建文件只在架构需要时（新模块/新页面）
- 不删除用户已有代码，除非明确要求
- 每次改动后提示用户在 Android Studio 中 Sync & Run

## ⚠️ API 契约变更流程

服务器（GPT）与客户端（Cursor）分支独立开发，靠 unified_api_contract_v2.md 对齐。

- 写网络层代码时，必须打开契约文档核对字段和路径
- **绝不自行修改 API 路径、字段名、消息类型**
- 若发现契约有问题（缺字段、歧义、前后矛盾），立即停下来高亮告知用户
- 用户确认后才能继续，必要时同步更新契约文档
